{
    "chat.tools.terminal.autoApprove": {
        "/^node --version; npm --version; rustc --version; solana --version; anchor --version$/": {
            "approve": true,
            "matchCommandLine": true
        }
    }
}